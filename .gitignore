# Live credentials — never commit
# config.php is tracked but secret-free; real secrets go in these (never committed):
api/config.secret.php
api/config.local.php

# Local dev upload scratch (production uploads/ lives on the server)
uploads/*
!uploads/.gitkeep

# Node / build output
node_modules/
app/node_modules/
app/dist/
dist/
public_html/

# OS / editor noise
.DS_Store
*.log
.hallmark/

# dev-only built-site test harness
router.dev.php
