# Never execute uploaded files — serve as inert downloads only.
php_flag engine off
<IfModule mod_rewrite.c>
    RewriteEngine Off
</IfModule>
<FilesMatch "\.(php|phtml|php3|php4|php5|php7|phar|pl|py|cgi|asp|sh)$">
    Require all denied
</FilesMatch>
